# Welcome to execution-service

## Installation
Make sure you have node and serverless framework installed globally. <br>
Run:
* npm install
* To get localstack up and running please look in jitsecurity/jit-ops repo.
* sls deploy --stage local -v
